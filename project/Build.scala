import sbt._
import Keys._

object Flow extends Build {
  /*
  lazy val mappableMacro = Project(id="mappable-macro", base = file("mappable-macro")) settings (
    libraryDependencies += "org.scala-lang" % "scala-reflect" % scalaVersion.value
  )
  */

}
